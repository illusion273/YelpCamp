<% layout('layouts/boilerplate') %>
    <!-- my-auto mx-auto -->
    <div class="d-flex flex-wrap my-auto flex-grow-1 py-sm-3">
        <div class="card shadow rounded flex-grow-1 m-auto" style="max-width: 420px;">
            <img src="<%= campground.image %>" class="card-img-top" alt="">
            <div class="card-body">
                <h5 class="card-title">
                    <%= campground.title %>
                </h5>
                <p class="card-text">
                    <%= campground.description %>

                </p>
            </div>
            <ul class="list-group list-group-flush">
                <li class="list-group-item">
                    <%= campground.location %>
                </li>
                <li class="list-group-item">
                    <%= campground.price %>
                </li>
            </ul>
            <div class="card-body">
                <a href="/campgrounds/<%= campground._id %>/edit" class="btn btn-warning">Edit</a>
                <form class="d-inline" action="/campgrounds/<%= campground.id %>?_method=DELETE" method="POST">
                    <button class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div>
        <div class="px-3 py-3 shadow rounded flex-grow-1 mx-auto" style="max-width: 420px;">
            <h3>Reviews</h3>
            <div class="my-auto overflow-auto" style="height: 250px;">
                <% for(let review of campground.reviews) { %>
                    <div class="mt-2">
                        <h5>Review</h5>
                        <p>Rating: <%= review.rating %></p>
                        <p><%= review.body %></p>
                    </div>
                    <% } %>
            </div>

            <form action="/campgrounds/<%= campground._id %>/reviews" method="POST">
                <div>
                    <label class="form-label" for="rating">Rating</label>
                    <input class="form-range" name="review[rating]" id="rating" type="range">
                </div>
                <div>
                    <label class="form-label" for="body">Review</label>
                    <textarea class="form-control" name="review[body]" id="body" cols="4" rows="6"></textarea>
                </div>
                <div class="mt-3 text-center">
                    <button class="btn btn-primary" type="submit">Submit</button>
                </div>

            </form>
        </div>
    </div>
    <!-- <div class="container my-auto mx-auto">
        <div class="row row-cols-1 row-cols-lg-2 bg-warning">
            <div class="col">
                <div class="card shadow rounded" style="max-width:420px;">
                    <img src="<%= campground.image %>" class="card-img-top" alt="">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= campground.title %>
                        </h5>
                        <p class="card-text">
                            <%= campground.description %>

                        </p>
                    </div>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">
                            <%= campground.location %>
                        </li>
                        <li class="list-group-item">
                            <%= campground.price %>
                        </li>
                    </ul>
                    <div class="card-body">
                        <a href="/campgrounds/<%= campground._id %>/edit" class="btn btn-warning">Edit</a>
                        <form class="d-inline" action="/campgrounds/<%= campground.id %>?_method=DELETE" method="POST">
                            <button class="btn btn-danger">Delete</button>
                        </form>
                    </div>
                </div>
            </div>
            <div class="col align-self-center">
                <div class="px-3 shadow rounded" style="max-width:420px;">
                    <form action="" class="py-3">
                        <div class=>
                            <label class="form-label" for="rating">Rating</label>
                            <input class="form-range" name="review[rating]" id="rating" type="range">
                        </div>
                        <div class="">
                            <label class="form-label" for="body">Review</label>
                            <textarea class="form-control" name="review[body]" id="body" cols="30" rows="6"></textarea>
                        </div>
                        <div class="mt-3 text-center">
                            <button class="btn btn-primary" type="submit">Submit</button>
                        </div>

                    </form>
                </div>
            </div>
        </div>
        
    </div> -->